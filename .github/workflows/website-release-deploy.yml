name: Website Production

on:
  workflow_call:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    name: Deploy Production
    runs-on: ubuntu-latest
    env:
      VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build website
        run: pnpm --filter @dev-to/website build

      - name: Deploy to Vercel (Production)
        id: deploy
        working-directory: packages/website
        run: |
          output=$(pnpm dlx vercel@latest deploy --prod --yes --token "$VERCEL_TOKEN" 2>&1)
          echo "$output"
          url=$(echo "$output" | grep -Eo 'https://[^ ]+' | tail -n 1)
          echo "url=$url" >> "$GITHUB_OUTPUT"

      - name: Get version info
        id: version
        run: |
          version=$(node -e "console.log(require('./packages/website/package.json').version)")
          echo "current=$version" >> "$GITHUB_OUTPUT"

          # Check if tag exists
          if git tag -l "website-v$version" | grep -q .; then
            echo "tag_exists=true" >> "$GITHUB_OUTPUT"
          else
            echo "tag_exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Get changelog
        id: changelog
        run: |
          version="${{ steps.version.outputs.current }}"
          last_tag=$(git tag --list 'website-v*' --sort=-v:refname | head -n 1)

          if [ -n "$last_tag" ]; then
            changelog=$(git log ${last_tag}..HEAD --pretty=format:"- %s" -- packages/website 2>/dev/null || echo "No changes")
          else
            changelog=$(git log --pretty=format:"- %s" -- packages/website 2>/dev/null | head -20 || echo "Initial release")
          fi

          echo "content<<EOF" >> "$GITHUB_OUTPUT"
          echo "$changelog" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        if: steps.version.outputs.tag_exists != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: website-v${{ steps.version.outputs.current }}
          name: Website v${{ steps.version.outputs.current }}
          generate_release_notes: false
          body: |
            ## 🌐 Website v${{ steps.version.outputs.current }}

            **生产环境:** ${{ steps.deploy.outputs.url }}

            ### 📝 更新内容

            ${{ steps.changelog.outputs.content }}

      - name: Summary
        run: |
          echo "### 🚀 Production Deployed" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "| Field | Value |" >> "$GITHUB_STEP_SUMMARY"
          echo "|-------|-------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Version | \`v${{ steps.version.outputs.current }}\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| URL | ${{ steps.deploy.outputs.url }} |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Tag | \`website-v${{ steps.version.outputs.current }}\` |" >> "$GITHUB_STEP_SUMMARY"
